import { createApp } from 'vue'
import './assets/variables.css' // Corrigido o caminho e o nome da pasta
import App from './App.vue'      // Removido o 'im' extra e corrigido o caminho

createApp(App).mount('#app')